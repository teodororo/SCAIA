{
  "name": "scaia",
  "version": "1.0.0",
  "description": "GitHub Action that runs an AI-powered code review on pull request diffs using any OpenAI-compatible endpoint.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "typecheck": "tsc --noEmit",
    "all": "npm run typecheck && npm run build"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.5.0"
  }
}
